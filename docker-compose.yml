# Placeholder. Docker is not required yet — `uv run uvicorn gateway.main:app --reload`
# is enough. Fill in runtime: nvidia / deploy.resources when the self-hosted GPU
# provider lands.
services:
  gateway:
    build: .
    env_file: .env
    ports:
      - "8000:8000"
    command: uvicorn gateway.main:app --host 0.0.0.0 --port 8000
